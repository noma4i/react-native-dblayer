"use strict";

/** Capture causality before transport starts. */
export const captureApplyState = (entityClock, scopeGeneration) => ({
  entityClock,
  scopeGeneration
});
//# sourceMappingURL=capture.js.map